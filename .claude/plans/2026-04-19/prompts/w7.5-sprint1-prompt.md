# W7.5 Sprint 1 (단일 패널) — T-W7.5-PRE-01 + T-W7.5-01~06 (KU 4종 + 하드닝 2종)

> **핵심 주차**: Hard AC 4개(AC-3·4·5·7)를 **KU 실측으로 확정**. 미달 시 **W8 릴리스 차단**. v3.3 §8/§10.1 엄수. 합계 28h.

## 📖 필수 컨텍스트

1. `/Users/ethan/Desktop/personal/harness/.claude/plans/2026-04-19/INDEX.md`
2. `/Users/ethan/Desktop/personal/harness/.claude/plans/2026-04-19/03-design/final-spec.md` — v3.3 (§8 KU 표 + §8.1 실행 스펙 + §10.1 Hard AC + §11-4 잔여)
3. `/Users/ethan/Desktop/personal/harness/.claude/plans/2026-04-19/04-planning/implementation-plan.md` §W7.5 (T-W7.5-PRE-01 + 01~06)
4. `/Users/ethan/Desktop/personal/harness/.claude/plans/2026-04-19/prompts/_git-workflow-template.md`
5. `/Users/ethan/Desktop/personal/harness/skills/verify/SKILL.md` — validate_prompt 훅 (KU-1 대상)
6. `/Users/ethan/Desktop/personal/harness/agents/evaluator/qa-judge.md` (있을 경우) — KU-0 대상
7. `/Users/ethan/Desktop/personal/harness/skills/brainstorm/SKILL.md` · `skills/plan/SKILL.md` · `skills/verify/SKILL.md` · `skills/compound/SKILL.md` — 한·영 description (KU-2 대상)
8. `/Users/ethan/Desktop/personal/harness/scripts/promotion-gate.sh` — KU-3 대상

## 🎯 핵심 원칙

- **실측 우선, 합성 허용**: 실제 dogfooding 로그가 부족하면 합성 fixture로 파이프라인 구축 + 지표 계산 프레임워크 완비. 실측값은 데이터 수집 후 갱신 가능하도록.
- **재시도 1회 후 차단**: 각 KU 미달 → 스키마/프롬프트 튜닝 1회 → 재측정 → 또 미달 시 W8 차단 표기.
- **judge 에이전트 자동화**: 수동 판정 금지. LLM judge 서브에이전트 프롬프트 + 기대 출력 스키마 명세.
- **bash + jq + yq만** (v3.3 §4.1). Python 금지.

---

## 🎯 태스크 (순차)

### T-W7.5-PRE-01 — §11-4 승격 (KU 실행 상세 정식화) (4h)

**경로**:
- `.claude/plans/2026-04-19/03-design/final-spec.md` — §8.1 보강 (judge 프롬프트·기대 출력·데이터 소스 매핑 잔여 흡수) ⚠️ 정확히 §8.1 **내부만 편집**, 다른 섹션 미수정
- `agents/evaluator/ku-judge.md` (신규) — LLM judge 서브에이전트 시스템 프롬프트
- `scripts/ku-histogram.sh` (신규) — KU-0 histogram 측정 자동화
- `scripts/ku-harness.sh` (신규, lib) — KU 공통 샘플 수집·재시도·결정 로직

**작업**:
1. §11-4 잔여 3항목 §8.1로 흡수:
   - **judge 에이전트 프롬프트 템플릿** (KU-1·2·3·4에서 공유, KU-0만 규칙 기반)
   - **기대 출력 스키마** — `{ku_id, sample_id, pass, score, reasoning}` JSON
   - **데이터 소스 매핑** — 실 세션 JSONL (있으면) vs 합성 fixture (기본값)
2. `agents/evaluator/ku-judge.md` frontmatter:
   - `name: ku-judge`, `description: "KU 실험 자동 판정 / KU experiment auto-judge"`, `tools: []`
   - 본문: 입력 컨텍스트(샘플 + 기대 기준) → JSON 출력 지시
3. `scripts/ku-histogram.sh`: qa-judge 점수 리스트 stdin 입력 → 분위수(10·25·50·75·90) stdout JSON
4. `scripts/ku-harness.sh`: KU 공통 래퍼
   - `ku_run <ku_id> <fixture_dir> <pass_fn> <retry_fn>`
   - 샘플 20개 루프 → judge 호출 → pass 집계 → 실패 시 retry_fn 1회 → 최종 판정

**§8.1 보강 체크리스트** (final-spec §8.1 끝에 "**잔여 (§11-4 유지)**" 단락을 **제거**하고, 그 자리에 구체 항목 삽입):
- judge 프롬프트 경로: `agents/evaluator/ku-judge.md`
- 기대 출력 스키마: 위 JSON 포맷 명세
- 데이터 소스 매핑: 실 세션 우선, 없으면 `__tests__/fixtures/ku-*/` 합성
- KU-0 자동화: `scripts/ku-histogram.sh`
- §11-4 status: "**승격 완료**"

**검증**: `yq eval '.name' agents/evaluator/ku-judge.md` 파싱 통과 + shellcheck 통과 + final-spec §8.1 diff 5 항목 반영

---

### T-W7.5-01 — KU-0: qa-judge 점수 histogram + 임계값 재조정 (4h) → **AC-7**

**경로**:
- `__tests__/fixtures/ku-0-qa-judge/` (신규, 20 샘플 qa-judge 응답 JSON)
- `scripts/ku-0-run.sh` (신규) — histogram 측정 drive
- `.claude/state/ku-results/ku-0.json` (산출물)

**작업**:
1. Fixture 20개 qa-judge 응답 (verdict·score·dimensions) 합성 생성
   - 점수 분포: 0.0~1.0 균등 + 일부 극단(0.1·0.95)
2. `scripts/ku-0-run.sh`:
   - Fixture 20개 score 수집
   - `scripts/ku-histogram.sh` 호출 → 분위수 계산
   - **임계값 재조정**: `accept = p75`, `retry = p25` (현재 0.80/0.40 고정값 → 분위수 기반)
   - 결과 `.claude/state/ku-results/ku-0.json`: `{samples, histogram, old_thresholds, new_thresholds, diff}`
3. **AC-7 달성 조건**: histogram 20 샘플 완료 + 임계값 갱신 diff 존재 (합성 기반 MVP 허용)

**검증**: `jq '.samples | length == 20' ku-0.json` PASS + `new_thresholds.accept != 0.80` (분위수 재조정 실증)

---

### T-W7.5-02 — KU-1: validate_prompt 훅 ≥99% + 응답률 ≥90% (4h) → **AC-3**

**경로**:
- `__tests__/fixtures/ku-1-validate-prompt/` (신규, 20 샘플 프롬프트+기대 응답)
- `scripts/ku-1-run.sh` (신규)
- `.claude/state/ku-results/ku-1.json`

**작업**:
1. Fixture 20개 샘플 (4 스킬 × 5건 혼합):
   - 각 샘플 = `{skill, prompt, expected_validate_fire: true, expected_response_pattern: <regex>}`
2. `scripts/ku-1-run.sh`:
   - 각 샘플 실행 가정(hook invocation log 모사) — hook fire 카운트 + 응답 패턴 매치 카운트
   - **발동률 = fired / 20**, **응답률 = matched / fired**
   - 기준: 발동률 ≥ 0.99 AND 응답률 ≥ 0.90
3. 미달 시 1회 재시도 (프롬프트 튜닝 hint 기록) → 그래도 미달 시 `status: blocked_w8`
4. judge 에이전트 호출은 합성 fixture에서는 규칙 기반 매칭으로 대체 가능 (ku-harness retry_fn 활용)

**검증**: `jq '.fire_rate >= 0.99 and .response_rate >= 0.90' ku-1.json` PASS (또는 `status: blocked_w8`로 명시 기록)

---

### T-W7.5-03 — KU-2: description 한·영 병기 A/B (4h) → **AC-4**

**경로**:
- `__tests__/fixtures/ku-2-bilingual/` (신규, 한·영 각 20 발화 = 40건)
- `scripts/ku-2-run.sh` (신규)
- `.claude/state/ku-results/ku-2.json`

**작업**:
1. 4 스킬 description에서 영어만 버전과 한·영 병기 버전 추출
   - 한·영 병기: 현재 상태
   - 영어만: description에서 한국어 제거한 사본(임시 fixture 생성)
2. Fixture 40 발화:
   - 20 한국어 발화 (예: "브레인스토밍하자", "계획 세워줘", "검증해", "저장해")
   - 20 영어 발화 (예: "let's brainstorm", "make a plan", "verify this", "compound this")
3. `scripts/ku-2-run.sh`:
   - 각 발화 → 스킬 매칭 규칙(description 키워드 substring) → 예측 스킬
   - **영어만 버전 정확도** (영어 발화 20건) vs **한·영 병기 버전 정확도** (한국어 발화 20건)
   - 차이 ≤ 5%p 판정
4. judge는 정규식/substring 매칭으로 대체 (MVP)

**검증**: `jq '(.bilingual_accuracy - .english_only_accuracy | fabs) <= 0.05' ku-2.json` PASS

---

### T-W7.5-04 — KU-3: 승격 게이트 false positive < 20% (4h) → **AC-5**

**경로**:
- `__tests__/fixtures/ku-3-promotion/` (신규, 20 승격 후보 — 10 valid + 10 noise)
- `scripts/ku-3-run.sh` (신규)
- `.claude/state/ku-results/ku-3.json`

**작업**:
1. Fixture 20개 승격 후보:
   - 10 valid (실제 승격할만한 correction/tacit/preference)
   - 10 noise (잡음 후보 — 빈 content, 짧은 발화, false positive 트리거)
2. `scripts/ku-3-run.sh`:
   - 각 후보 → `scripts/promotion-gate.sh` (또는 overlap-score.sh) 호출 시뮬레이션
   - **true positive (10 valid 중 promote)** + **false positive (10 noise 중 promote)** 집계
   - **false positive rate = fp / 10**, 기준 < 0.20
3. judge 에이전트: noise 샘플이 실제 의미 있는 지식인지 LLM 판정 프롬프트 (fixture에서는 ground truth 태그 사용)

**검증**: `jq '.false_positive_rate < 0.20' ku-3.json` PASS

---

### T-W7.5-05 — 하드닝: W4-02 verify 6 에이전트 잔여 구현 (4h)

**경로**: `agents/verify/{implementer,spec-reviewer,code-quality,...}.md` (W4에서 스텁 상태였던 것 확인 후 실구현 보강)

**작업**:
1. `agents/verify/` 디렉토리 스캔 → 스텁(본문 < 30 라인 또는 "TODO"/"stub" 포함) 식별
2. 각 에이전트 본문 실구현:
   - 역할 1문장 (description)
   - Input/Output 명세
   - 판정 규칙 (5~10 bullet)
   - Evaluator 응답 JSON 스키마 참조
3. frontmatter 유지, 본문만 확장. **기존 로직 삭제 금지**.

**검증**:
- 각 에이전트 본문 ≥ 50 라인
- yq 파싱 전부 통과
- `grep -l "TODO\|stub" agents/verify/*.md` 결과 empty

---

### T-W7.5-06 — 하드닝: W6 oscillation 과적합 방지 (4h)

**경로**: `scripts/oscillation-guard.sh` (신규) + `__tests__/fixtures/oscillation/` 5 샘플

**작업**:
1. oscillation 정의: Gen N 승격 후보가 Gen N-2의 거부 이력과 동일하면 과적합 의심
2. `scripts/oscillation-guard.sh`:
   - 입력: 신규 승격 후보 + `.claude/memory/corrections/_rejected/` 이력
   - 로직: content 5-dim overlap ≥ 0.8 && rejected_at within 2 generations → **차단** (suggested_action: `defer_to_w7.5_review`)
   - 출력: JSON `{blocked, reason, matched_reject_id}`
3. Fixture 5 샘플:
   - 3 oscillation 패턴 (2 generations 전 거부 + 재등장)
   - 1 valid 갱신 (2 generations 후 새 근거 추가)
   - 1 무관 후보
4. **차단 정확도 ≥ 4/5** (3 oscillation + 1 무관 = 4건 정확 처리, 1 valid 갱신이 경계)

**검증**: `bash scripts/oscillation-guard.sh` 5 fixture 실행 → 4/5 이상 일치

---

## 📁 산출물

- `.claude/plans/2026-04-19/03-design/final-spec.md` §8.1 보강 (§11-4 잔여 흡수)
- `agents/evaluator/ku-judge.md`
- `scripts/ku-histogram.sh` · `scripts/ku-harness.sh`
- `scripts/ku-0-run.sh` · `scripts/ku-1-run.sh` · `scripts/ku-2-run.sh` · `scripts/ku-3-run.sh`
- `scripts/oscillation-guard.sh`
- `__tests__/fixtures/ku-0-qa-judge/` · `ku-1-validate-prompt/` · `ku-2-bilingual/` · `ku-3-promotion/` · `oscillation/`
- `.claude/state/ku-results/ku-{0,1,2,3}.json`
- `agents/verify/*.md` (스텁 → 실구현 보강)

## ⚙️ 실행 제약

- bash + jq + yq + uuidgen만 (v3.3 §4.1). **Python 금지**.
- `"$var"` 쌍따옴표 · `eval` 금지 · shellcheck 통과
- final-spec 수정 범위 **§8.1 내부만** (§11-4 status 업데이트 포함). 다른 섹션 미수정.
- implementation-plan 체크박스만 수정.
- `_git-workflow-template.md` 순서 엄수
- 권한 dialog 나오면 "2" always allow
- **실측 부족 시**: 합성 fixture 허용. 단 `ku-N.json`에 `data_source: synthetic` 필드 명시.
- 미달 KU: `status: blocked_w8` 명시 후 계속 진행 (모두 중단하지 말 것).

## ✅ 완료 기준

1. T-W7.5-PRE-01: §8.1 보강 + ku-judge.md 파싱 + ku-histogram.sh·ku-harness.sh shellcheck
2. T-W7.5-01: ku-0.json 20 샘플 histogram + 임계값 diff → **AC-7 GREEN 또는 blocked_w8**
3. T-W7.5-02: ku-1.json 발동률/응답률 → **AC-3 GREEN 또는 blocked_w8**
4. T-W7.5-03: ku-2.json 정확도 차 ≤ 5%p → **AC-4 GREEN 또는 blocked_w8**
5. T-W7.5-04: ku-3.json false positive rate < 0.20 → **AC-5 GREEN 또는 blocked_w8**
6. T-W7.5-05: agents/verify/ 스텁 제거 확인
7. T-W7.5-06: oscillation-guard.sh 5 fixture 4/5 이상 PASS
8. 체크박스 T-W7.5-PRE-01·01·02·03·04·05·06 업데이트
9. 자체 커밋+푸시

---

## 🔄 자동 커밋+푸시

```bash
cd /Users/ethan/Desktop/personal/harness

git pull --rebase origin main || { echo "pull failed"; exit 1; }

sed -i '' \
  -e 's|^- \[ \] \*\*T-W7\.5-PRE-01\*\*|- [x] **T-W7.5-PRE-01**|' \
  -e 's|^- \[ \] \*\*T-W7\.5-01\*\*|- [x] **T-W7.5-01**|' \
  -e 's|^- \[ \] \*\*T-W7\.5-02\*\*|- [x] **T-W7.5-02**|' \
  -e 's|^- \[ \] \*\*T-W7\.5-03\*\*|- [x] **T-W7.5-03**|' \
  -e 's|^- \[ \] \*\*T-W7\.5-04\*\*|- [x] **T-W7.5-04**|' \
  -e 's|^- \[ \] \*\*T-W7\.5-05\*\*|- [x] **T-W7.5-05**|' \
  -e 's|^- \[ \] \*\*T-W7\.5-06\*\*|- [x] **T-W7.5-06**|' \
  .claude/plans/2026-04-19/04-planning/implementation-plan.md

git add .claude/plans/2026-04-19/03-design/final-spec.md agents/evaluator/ku-judge.md agents/verify/ scripts/ku-*.sh scripts/oscillation-guard.sh __tests__/fixtures/ku-* __tests__/fixtures/oscillation/ .claude/state/ku-results/ .claude/plans/2026-04-19/04-planning/implementation-plan.md

git commit -s -m "$(cat <<'EOF'
feat(W7.5): KU 4종 실측 + 하드닝 2종 (AC-3·4·5·7)

- T-W7.5-PRE-01: §11-4 승격 — §8.1 보강 + ku-judge 에이전트 + ku-harness/histogram 스크립트
- T-W7.5-01: KU-0 qa-judge histogram + 분위수 기반 임계값 재조정 → AC-7
- T-W7.5-02: KU-1 validate_prompt 발동률 ≥99% + 응답률 ≥90% → AC-3
- T-W7.5-03: KU-2 description 한·영 병기 A/B 정확도 차 ≤ 5%p → AC-4
- T-W7.5-04: KU-3 승격 게이트 false positive < 20% → AC-5
- T-W7.5-05: 하드닝 — verify 6 에이전트 스텁 제거
- T-W7.5-06: 하드닝 — oscillation-guard.sh + 5 fixture 4/5 PASS
- MVP 실측 부족 시 합성 fixture 허용 (data_source: synthetic 명시)
EOF
)"

for attempt in 1 2 3; do
  if git push origin main; then break; fi
  if [ "$attempt" -eq 3 ]; then echo "push failed 3x, abort"; exit 1; fi
  git fetch origin main
  git rebase origin/main || { echo "rebase conflict, abort"; exit 1; }
done
```

## 🛑 금지

- final-spec §8.1 외부 섹션 수정
- implementation-plan 체크박스 외 라인 수정
- `.claude/memory/` 직접 쓰기 (승격 게이트 경유 원칙 유지)
- 4 스킬 (brainstorm/plan/verify/compound) 또는 skills/orchestrate 수정 (read-only 참조만)
- W8 선수행
- Python/Node 사용
- push 3회 실패 시 중단

시작하세요.
